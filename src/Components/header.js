import React from 'react'

const Header = (props)=>{
    return (
        <header>
            <div>expandable</div>
            <h4>Quintype</h4>
            <i></i>
        </header>
        )
}

export default Header
